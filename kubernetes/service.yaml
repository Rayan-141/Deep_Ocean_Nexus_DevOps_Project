apiVersion: v1
kind: Service
metadata:
  name: deepocean-service
  namespace: deepocean
spec:
  type: NodePort
  selector:
    app: deepocean-app
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30000
